import { usersQuery } from './usersQuery';
import { meQuery } from './meQuery';
import { userQuery } from './userQuery';

export const Query = {
  users: usersQuery,
  me: meQuery,
  user: userQuery,
};
